import type { Activity } from './types'

export const activities: Activity[] = [
  // TODO: notes mention "boat", "silves jeeps", "kayak", and "horses" as
  // Algarve adventure-day options. None has a fixed provider/location yet —
  // add proper entries once we pick specific tours, otherwise they'd be
  // markers with made-up coordinates.
]
